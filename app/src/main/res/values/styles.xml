<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PhotoListItemAuthor">
        <item name="android:textColor">@color/photo_list_item_black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="PhotoListItemTag">
        <item name="android:textColor">@color/photo_list_item_tag</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="PhotoListItemTitle">
        <item name="android:textColor">@color/photo_list_item_black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="PhotoListItemContent">
        <item name="android:textColor">@color/photo_list_item_black</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
